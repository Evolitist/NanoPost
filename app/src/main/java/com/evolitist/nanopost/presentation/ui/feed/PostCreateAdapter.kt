package com.evolitist.nanopost.presentation.ui.feed

/*import android.view.LayoutInflater
import android.view.ViewGroup
import androidx.recyclerview.widget.RecyclerView
import com.evolitist.nanopost.databinding.ItemCreatePostBinding

class PostCreateAdapter(
    private val onClick: () -> Unit,
) : RecyclerView.Adapter<PostCreateAdapter.ViewHolder>() {

    override fun onCreateViewHolder(parent: ViewGroup, viewType: Int): ViewHolder {
        return ViewHolder(
            ItemCreatePostBinding.inflate(
                LayoutInflater.from(parent.context),
                parent,
                false,
            )
        )
    }

    override fun onBindViewHolder(holder: ViewHolder, position: Int) {
        holder.itemView.setOnClickListener { onClick() }
    }

    override fun getItemCount() = 1

    class ViewHolder(binding: ItemCreatePostBinding) : RecyclerView.ViewHolder(binding.root)
}*/
